version: '3.8'
services:
  app:
    build: .
    ports:
      - "3000:3000"
    working_dir: /project/frontend
    command: npm run start
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3000"]
      interval: 30s
      timeout: 10s
      retries: 3
